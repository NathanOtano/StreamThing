{
  "tool": "StreamThing",
  "version": "0.1.2",
  "distribution": "source plus Windows installer and CLI assets",
  "public_surface": {
    "repository_name": "StreamThing",
    "release_tag": "v0.1.2"
  },
  "published_asset_classes": [
    "source-archive",
    "windows-nsis-installer",
    "standalone-windows-cli",
    "sha256-checksums"
  ],
  "privacy_model": {
    "runtime_configuration": "Syncthing URL, API key, folders and devices are read from the user's local Syncthing config.xml at runtime.",
    "repository_includes": [
      "application source",
      "build scripts",
      "documentation",
      "placeholder examples"
    ],
    "repository_excludes": [
      "credentials",
      "personal data",
      "local filesystem paths",
      "runtime state",
      "dependency folders",
      "build outputs"
    ]
  },
  "release_gates": [
    {
      "name": "source-tree-scan",
      "required": true,
      "evidence": "targeted scans for credentials, local paths and machine-specific identifiers before publication"
    },
    {
      "name": "build-and-test",
      "required": true,
      "evidence": "file-tree smoke test, web build, CLI build and desktop bundle build"
    },
    {
      "name": "binary-scan",
      "required": true,
      "evidence": "release assets scanned for local path and machine-specific strings"
    },
    {
      "name": "install-check",
      "required": true,
      "evidence": "installer run and installed streamthing command verified"
    },
    {
      "name": "checksums",
      "required": true,
      "evidence": "release folder contains SHA256SUMS.txt"
    }
  ],
  "known_limits": [
    "The Windows installer is unsigned.",
    "StreamThing requires a local Syncthing installation.",
    "StreamThing is a desktop companion, not a synchronization engine."
  ]
}
